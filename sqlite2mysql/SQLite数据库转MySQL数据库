# Remove data from MySQL

mdb="memos22"
mysql -u root -D "$mdb" -e "delete from user"
# mysql -u root -D "$mdb" -e "delete from tag"
mysql -u root -D "$mdb" -e "delete from memo"
mysql -u root -D "$mdb" -e "delete from resource"
mysql -u root -D "$mdb" -e "delete from memo_organizer"
mysql -u root -D "$mdb" -e "delete from memo_relation"
mysql -u root -D "$mdb" -e "delete from reaction"
mysql -u root -D "$mdb" -e "delete from inbox"
